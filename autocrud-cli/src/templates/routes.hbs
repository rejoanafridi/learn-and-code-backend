import express from 'express';
import {
  create{{resourceNamePascalCase}},
  getAll{{resourceNamePascalCase}}s,
  get{{resourceNamePascalCase}}ById,
  update{{resourceNamePascalCase}},
  delete{{resourceNamePascalCase}},
} from '../controllers/{{resourceNameLowercase}}.controller'; // Adjust path as needed

// import { protect, authorize } from '../middleware/auth'; // Example for auth middleware

const router = express.Router();

router
  .route('/')
  // .post(protect, authorize('admin'), create{{resourceNamePascalCase}}) // Example with auth
  .post(create{{resourceNamePascalCase}})
  .get(getAll{{resourceNamePascalCase}}s);

router
  .route('/:id') // Using generic ':id' as is common practice
  .get(get{{resourceNamePascalCase}}ById)
  // .put(protect, authorize('admin'), update{{resourceNamePascalCase}}) // Example with auth
  .put(update{{resourceNamePascalCase}})
  // .delete(protect, authorize('admin'), delete{{resourceNamePascalCase}}); // Example with auth
  .delete(delete{{resourceNamePascalCase}});

export default router;
